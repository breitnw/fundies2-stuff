package lists;

public class AppendLoString implements ILoString {
  public AppendLoString(ILoString left, ILoString right) {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }

  @Override
  public ILoString reverse() {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }

  @Override
  public ILoString normalize() {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }

  @Override
  public ILoString scanConcat() {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }
}
