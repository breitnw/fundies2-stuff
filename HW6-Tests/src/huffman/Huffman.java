package huffman;

import java.util.ArrayList;

public class Huffman {
  public Huffman(ArrayList<String> strs, ArrayList<Integer> frequency) {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }
  
  public String decode(ArrayList<Boolean> toDecode) {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }
    
  public ArrayList<Boolean> encode(String toEncode) {
    throw new RuntimeException("If you got this to run, your tests compile correctly against the data definitions.");
  }
}
